import {readFile} from 'node:fs/promises';import path from 'node:path';
export async function validateSecurity({root,finding}){const findings=[];const adapter=await readFile(path.join(root,'src/js/35-persistence-adapter.js'),'utf8');const storage=await readFile(path.join(root,'src/js/33-library-storage.js'),'utf8');const ai=await readFile(path.join(root,'src/js/40-ai-generation.js'),'utf8');if(!/enabled:false/.test(adapter)||!/baseUrl:''/.test(adapter))findings.push(finding('security','BLOCKER','SERVER_ENABLED','Bezserverové vydání nesmí mít aktivní server.'));if(!/^[\s\S]*\^\[=\+@-\]/m.test(storage))findings.push(finding('security','MAJOR','CSV_PROTECTION','Chybí ochrana CSV proti formula injection.'));if(/ACTIVA_SERVER_CONFIG[^]*enabled:true/.test(adapter))findings.push(finding('security','BLOCKER','SERVER_CONFIG','Serverový adaptér je aktivní.'));if(!/generativelanguage\.googleapis\.com/.test(ai))findings.push(finding('security','MINOR','AI_ENDPOINT','Nebyl potvrzen očekávaný Gemini endpoint.'));return{findings,details:{serverMode:'disabled',adapterSchema:'activa-persistence-adapter-v1',csvFormulaProtection:true}}}
