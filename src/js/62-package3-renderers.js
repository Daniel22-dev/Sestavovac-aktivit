function p3CardTitle(label,title){return`<div class="p3-card-title"><span>${esc(label)}</span><b>${esc(title)}</b></div>`}
function renderStations(activity,mode){const stations=activity.data.stations||[];if(activity.data._answerSheetOnly)return mode==='student'?`<section class="station-answer-sheet station-answer-sheet-only"><h3>${esc(activity.data.answerSheetTitle||'Odpovědní arch')}</h3>${stations.map((station,i)=>`<div><b>${i+1}. ${esc(station.title)}</b><span></span></div>`).join('')}</section>`:`<section class="station-answer-sheet station-answer-sheet-only station-answer-key"><h3>Řešení stanovišť</h3>${stations.map((station,i)=>`<div><b>${i+1}. ${esc(station.title)}</b><span>${esc(station.answer||'—')}</span></div>`).join('')}</section>`;return`<div class="station-grid">${stations.map((station,i)=>`<section class="station-card">${p3CardTitle(`STANOVIŠTĚ ${Number(station._number||i+1)}`,station.title)}<div class="station-time">${Number(station.minutes||5)} min</div>${station.instruction?`<p class="station-instruction">${esc(station.instruction)}</p>`:''}<div class="station-task">${esc(station.task)}</div>${mode==='key'?`<div class="key-answer"><b>Řešení:</b> ${esc(station.answer||'—')}</div>`:'<div class="station-answer-lines"></div>'}</section>`).join('')}</div>`}
function renderJigsaw(activity,mode){const groups=activity.data.groups||[];return`<div class="jigsaw-grid">${groups.map(group=>`<section class="jigsaw-card"><div class="jigsaw-badge">SKUPINA ${esc(group.label)}</div><div class="jigsaw-source">${esc(group.source).replace(/\n/g,'<br>')}</div><div class="jigsaw-questions">${(group.questions||[]).map((q,i)=>`<div><b>${i+1}. ${esc(q.question)}</b>${mode==='key'?`<p>${esc(q.answer||'—')}</p>`:'<span></span>'}</div>`).join('')}</div></section>`).join('')}</div>${activity.data.finalTask?`<div class="synthesis-box"><b>Společná syntéza</b>${esc(activity.data.finalTask)}${mode==='student'?'<div class="synthesis-lines"></div>':''}</div>`:''}`}
function renderExpertGroups(activity,mode){return`<div class="expert-grid">${(activity.data.groups||[]).map((group,i)=>`<section class="expert-card"><div class="expert-number">${i+1}</div><h3>${esc(group.role)}</h3><small>${esc(group.focus)}</small><div class="expert-material">${esc(group.material).replace(/\n/g,'<br>')}</div><div class="expert-deliverable"><b>Výstup</b>${esc(group.deliverable)}</div>${mode==='student'?'<div class="expert-notes"></div>':''}</section>`).join('')}</div>${activity.data.synthesis?`<div class="synthesis-box"><b>Společný výstup</b>${esc(activity.data.synthesis)}${mode==='student'?'<div class="synthesis-lines"></div>':''}</div>`:''}`}
function renderEscapeRoom(activity,mode){const d=activity.data;return`<div class="escape-intro"><span>MISE</span>${esc(d.intro)}</div><div class="lock-grid">${(d.locks||[]).map((lock,i)=>`<section class="lock-card"><div class="lock-head"><span>0${i+1}</span><b>${esc(lock.title)}</b></div><div class="lock-task">${esc(lock.task)}</div>${mode==='student'?'<div class="lock-answer"></div>':`<div class="key-answer"><b>Odpověď:</b> ${esc(lock.answer||'—')}</div>`}<div class="lock-code"><small>KÓD</small><strong>${mode==='key'?esc(lock.code):'□'}</strong></div>${lock.clue?`<details class="lock-clue" ${mode==='key'?'open':''}><summary>Nápověda</summary>${esc(lock.clue)}</details>`:''}</section>`).join('')}</div><div class="final-code"><span>FINÁLNÍ KÓD</span>${mode==='key'?`<b>${esc(d.finalCode)}</b>`:String(d.finalCode||'000').split('').map(()=>'<i></i>').join('')}</div>`}
function renderBoardGame(activity,mode,variant){const cells=activity.data.cells||[],shown=variant==='A'?cells:shuffle(cells,`${activity.id}-${variant}-board`).map((cell,i)=>({...cell,number:i+1}));return`<div class="board-rules"><b>Pravidla</b>${esc(activity.data.rules)}</div><div class="board-grid">${shown.map((cell,i)=>`<div class="board-cell ${String(cell.type||'').toLowerCase()}"><span>${i===0?'START':i===shown.length-1?'CÍL':cell.number}</span><small>${esc(cell.type)}</small><p>${esc(cell.prompt)}</p>${mode==='key'?`<b class="board-answer">${esc(cell.answer||'—')}</b>`:''}</div>`).join('')}</div><div class="board-kit"><span>Figurky: □ □ □ □</span><span>Kostka: 1–6</span></div>`}
function renderRoleCards(activity,mode){return`<div class="scenario-banner"><b>Společná situace</b>${esc(activity.data.scenario)}</div><div class="role-card-grid">${(activity.data.roles||[]).map((role,i)=>`<section class="role-card"><div class="cut-mark">✂</div><small>ROLE ${i+1}</small><h3>${esc(role.role)}</h3><div><b>Váš cíl</b>${esc(role.goal)}</div><div class="secret-role-info"><b>Neveřejná informace</b>${mode==='key'?esc(role.information):esc(role.information)}</div><div><b>Co máte udělat</b>${esc(role.prompt)}</div></section>`).join('')}</div>`}
function renderCaseStudy(activity,mode){const d=activity.data;return`<div class="case-file"><div class="case-file-tab">PŘÍPADOVÝ SPIS</div>${esc(d.caseText).replace(/\n/g,'<br>')}</div><div class="case-questions">${(d.questions||[]).map((q,i)=>`<section><b>${i+1}. ${esc(q.question)}</b>${mode==='key'?`<div class="key-answer">${esc(q.answer||'—')}</div>`:'<div class="answer-lines"></div>'}</section>`).join('')}</div>${d.decision?`<div class="decision-box"><b>Rozhodnutí skupiny</b>${esc(d.decision)}${mode==='student'?'<div class="decision-lines"></div>':''}</div>`:''}`}
function renderDebateCards(activity,mode){const d=activity.data;return`<div class="debate-motion"><small>DEBATNÍ TEZE</small><h2>${esc(d.motion)}</h2></div><div class="debate-columns"><section class="debate-side pro"><h3>PRO</h3>${(d.proArguments||[]).map((arg,i)=>`<div><span>${i+1}</span>${esc(arg)}</div>`).join('')}</section><section class="debate-side con"><h3>PROTI</h3>${(d.conArguments||[]).map((arg,i)=>`<div><span>${i+1}</span>${esc(arg)}</div>`).join('')}</section></div>${d.roles?.length?`<div class="debate-roles">${d.roles.map(role=>`<div><b>${esc(role.role)}</b><span>${esc(role.task)}</span></div>`).join('')}</div>`:''}${d.reflection?`<div class="reflection-box"><b>Reflexe</b>${esc(d.reflection)}${mode==='student'?'<div class="reflection-line"></div>':''}</div>`:''}`}
function visualFallback(kind='image'){const label=kind==='map'?'MAPOVÝ PODKLAD':kind==='diagram'?'DIAGRAM':'OBRÁZEK';return`<svg viewBox="0 0 900 520" class="visual-fallback" aria-label="${label}"><rect x="6" y="6" width="888" height="508" rx="22" fill="#f7f8f9" stroke="#9fa8b2" stroke-width="3" stroke-dasharray="12 9"/><path d="M150 370 L330 190 L445 300 L560 145 L760 370 Z" fill="#e4e8ec" stroke="#9aa4af" stroke-width="4"/><circle cx="280" cy="150" r="54" fill="#edf0f3" stroke="#9aa4af" stroke-width="4"/><text x="450" y="455" text-anchor="middle" font-family="Arial" font-size="28" fill="#69737e">${label} NAHRAJTE V EDITORU</text></svg>`}
function renderVisualCanvas(activity,mode,kind='image'){const d=activity.data||{},image=d.imageDataUrl?`<img src="${d.imageDataUrl}" alt="${esc(d.imageName||'Vizuální podklad')}">`:visualFallback(kind);return`<div class="visual-canvas ${kind}">${image}${(d.markers||[]).map((marker,i)=>`<span class="visual-marker ${mode==='key'?'key':''}" style="left:${p3Percent(marker.x)}%;top:${p3Percent(marker.y)}%" title="${esc(marker.answer||marker.prompt)}">${esc(marker.label||String(i+1))}</span>`).join('')}</div>`}
function renderImageAnnotation(activity,mode){return`${renderVisualCanvas(activity,mode,'image')}<table class="visual-answer-table"><thead><tr><th>#</th><th>Zadání</th><th>Odpověď</th></tr></thead><tbody>${(activity.data.markers||[]).map((m,i)=>`<tr><td>${esc(m.label||String(i+1))}</td><td>${esc(m.prompt)}</td><td>${mode==='key'?`<b>${esc(m.answer||'—')}</b>`:'____________________________'}</td></tr>`).join('')}</tbody></table>`}
function renderDiagramLabels(activity,mode){return`${renderVisualCanvas(activity,mode,'diagram')}<table class="visual-answer-table"><thead><tr><th>#</th><th>Název</th><th>Funkce / vysvětlení</th></tr></thead><tbody>${(activity.data.markers||[]).map((m,i)=>`<tr><td>${esc(m.label||String(i+1))}</td><td>${mode==='key'?`<b>${esc(m.answer||'—')}</b>`:'__________________'}</td><td>${mode==='key'?esc(m.function||'—'):'________________________________'}</td></tr>`).join('')}</tbody></table>`}
function renderBlankMap(activity,mode){const d=activity.data;return`${renderVisualCanvas(activity,mode,'map')}<div class="map-lower"><table class="visual-answer-table"><thead><tr><th>#</th><th>Zadání</th><th>Odpověď</th></tr></thead><tbody>${(d.markers||[]).map((m,i)=>`<tr><td>${esc(m.label||String(i+1))}</td><td>${esc(m.prompt)}</td><td>${mode==='key'?`<b>${esc(m.answer||'—')}</b>`:'__________________'}</td></tr>`).join('')}</tbody></table>${d.legend?.length?`<div class="map-legend"><b>Legenda</b>${d.legend.map(x=>`<span><i>${esc(x.symbol)}</i>${esc(x.meaning)}</span>`).join('')}</div>`:''}</div>`}
function dataGraphSvg(data){const rows=data.rows||[],w=760,h=330,p={l:58,r:22,t:24,b:64},values=rows.map(x=>Number(x.value)||0),min=Math.min(0,...values),max=Math.max(1,...values),range=max-min||1,x=(i)=>p.l+(i+.5)*(w-p.l-p.r)/Math.max(1,rows.length),y=(v)=>p.t+(max-v)*(h-p.t-p.b)/range,zero=y(0),ticks=5;const grid=Array.from({length:ticks+1},(_,i)=>{const value=min+(range/ticks)*i,yy=y(value);return`<line x1="${p.l}" y1="${yy}" x2="${w-p.r}" y2="${yy}" stroke="#d8dde2"/><text x="${p.l-8}" y="${yy+4}" text-anchor="end" font-size="12" fill="#5e6873">${Math.round(value*100)/100}</text>`}).join('');let marks='';if(data.chartType==='line'){const path=rows.map((row,i)=>`${i?'L':'M'}${x(i)},${y(Number(row.value)||0)}`).join(' ');marks=`<path d="${path}" fill="none" stroke="#28323c" stroke-width="4"/>${rows.map((row,i)=>`<circle cx="${x(i)}" cy="${y(Number(row.value)||0)}" r="5" fill="#28323c"/>`).join('')}`}else{const bw=Math.max(18,(w-p.l-p.r)/Math.max(1,rows.length)*.58);marks=rows.map((row,i)=>{const yy=y(Number(row.value)||0),top=Math.min(yy,zero),height=Math.max(2,Math.abs(zero-yy));return`<rect x="${x(i)-bw/2}" y="${top}" width="${bw}" height="${height}" rx="4" fill="#cbd2d9" stroke="#4f5a65"/>`}).join('')}return`<svg viewBox="0 0 ${w} ${h}" class="data-graph-svg" aria-label="Datový graf">${grid}<line x1="${p.l}" y1="${zero}" x2="${w-p.r}" y2="${zero}" stroke="#606a75" stroke-width="2"/>${marks}${rows.map((row,i)=>`<text x="${x(i)}" y="${h-p.b+22}" text-anchor="middle" font-size="12" fill="#38424c">${esc(row.label)}</text><text x="${x(i)}" y="${Math.max(16,y(Number(row.value)||0)-9)}" text-anchor="middle" font-size="12" font-weight="700">${Number(row.value)}</text>`).join('')}<text x="${w/2}" y="${h-8}" text-anchor="middle" font-size="13" fill="#5e6873">${esc(data.xLabel||'')}</text><text transform="translate(15 ${h/2}) rotate(-90)" text-anchor="middle" font-size="13" fill="#5e6873">${esc(data.yLabel||'')}</text></svg>`}
function renderDataGraph(activity,mode){const d=activity.data;return`<div class="graph-title">${esc(d.title)}</div><div class="graph-frame">${dataGraphSvg(d)}</div><div class="short-list graph-questions">${(d.questions||[]).map((q,i)=>`<div class="short-item"><b>${i+1}. ${esc(q.question)}</b>${mode==='key'?`<div class="key-answer">${esc(q.answer||'—')}</div>`:'<div class="answer-lines"></div>'}</div>`).join('')}</div>`}
function renderTimelineCards(activity,mode,variant){const events=mode==='key'?(activity.data.events||[]):shuffle(activity.data.events||[],`${activity.id}-${variant}-timeline`);return`<div class="timeline-ruler"><span>ZAČÁTEK</span><i></i><span>KONEC</span></div><div class="timeline-card-grid">${events.map((event,i)=>`<section class="timeline-card"><div class="cut-mark">✂</div><small>${mode==='key'?esc(event.date):`KARTA ${i+1}`}</small><h3>${esc(event.event)}</h3><p>${esc(event.description)}</p>${mode==='student'?'<div class="timeline-date">Datum / pořadí: __________</div>':''}</section>`).join('')}</div>`}

const package3OriginalRenderActivityContent=renderActivityContent;
renderActivityContent=function(activity,mode,variant){const renderer={stations:renderStations,jigsaw:renderJigsaw,expertgroups:renderExpertGroups,escaperoom:renderEscapeRoom,boardgame:renderBoardGame,rolecards:renderRoleCards,casestudy:renderCaseStudy,debatecards:renderDebateCards,imageannotation:renderImageAnnotation,diagramlabels:renderDiagramLabels,blankmap:renderBlankMap,datagraph:renderDataGraph,timelinecards:renderTimelineCards}[activity.type];return renderer?renderer(activity,mode,variant):package3OriginalRenderActivityContent(activity,mode,variant)};

function p3Chunk(items,size){const out=[];for(let i=0;i<items.length;i+=size)out.push(items.slice(i,i+size));return out}
function expandPackage3Activity(activity){
  if(activity.type==='stations'&&(activity.data.stations?.length||0)>4){
    const allStations=(activity.data.stations||[]).map((station,index)=>({...station,_number:index+1}));
    const pages=p3Chunk(allStations,4).map((stations,i,all)=>({...clone(activity),id:`${activity.id}-part-${i+1}`,title:`${activity.title} · ${i+1}/${all.length}`,data:{...clone(activity.data),stations,_answerSheetOnly:false}}));
    pages.push({...clone(activity),id:`${activity.id}-answer-sheet`,title:activity.data.answerSheetTitle||'Odpovědní arch',data:{...clone(activity.data),stations:allStations,_answerSheetOnly:true}});
    return pages;
  }
  if(activity.type==='jigsaw'&&(activity.data.groups?.length||0)>1)return(activity.data.groups||[]).map((group,i)=>({...clone(activity),id:`${activity.id}-group-${group.label||i+1}`,title:`${activity.title} · skupina ${group.label||i+1}`,data:{...clone(activity.data),groups:[group],finalTask:activity.data.finalTask}}));
  if(activity.type==='expertgroups'&&(activity.data.groups?.length||0)>3)return p3Chunk(activity.data.groups,3).map((groups,i,all)=>({...clone(activity),id:`${activity.id}-part-${i+1}`,title:`${activity.title} · ${i+1}/${all.length}`,data:{...clone(activity.data),groups,synthesis:activity.data.synthesis}}));
  if(activity.type==='rolecards'&&(activity.data.roles?.length||0)>4)return p3Chunk(activity.data.roles,4).map((roles,i,all)=>({...clone(activity),id:`${activity.id}-part-${i+1}`,title:`${activity.title} · ${i+1}/${all.length}`,data:{...clone(activity.data),roles}}));
  if(activity.type==='timelinecards'&&(activity.data.events?.length||0)>8)return p3Chunk(activity.data.events,8).map((events,i,all)=>({...clone(activity),id:`${activity.id}-part-${i+1}`,title:`${activity.title} · ${i+1}/${all.length}`,data:{...clone(activity.data),events}}));
  return[activity];
}
const package3OriginalProjectPages=projectPages;
projectPages=function(project,variant='A',mode='student',options={},level=project.activeLevel||'standard'){
  const hasPackage3=(project.activities||[]).some(activity=>PACKAGE3_ACTIVITY_REGISTRY[activity.type]);
  if(!hasPackage3)return package3OriginalProjectPages(project,variant,mode,options,level);
  const transformed=(project.activities||[]).map(activity=>differentiateActivity(variantActivity(activity,variant),project.differentiation?.enabled===false?(project.difficulty||level):level)).flatMap(expandPackage3Activity),modes=mode==='bundle'?['student','key']:[mode],total=transformed.length*modes.length;let page=0;return modes.flatMap(pageMode=>transformed.map((activity,index)=>{page++;return renderWorksheetPage(project,activity,index,variant,pageMode,options,page,total,level)}));
};
