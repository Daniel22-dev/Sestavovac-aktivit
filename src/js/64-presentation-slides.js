function presentationLevel(project=App.project){return project.differentiation?.enabled===false?(project.difficulty||'standard'):(project.activeLevel||'standard')}
function presentationPreparedActivities(project=App.project,variant=project.variant||'A',level=presentationLevel(project)){return(project.activities||[]).map((activity)=>differentiateActivity(variantActivity(activity,variant),project.differentiation?.enabled===false?(project.difficulty||level):level)).flatMap((activity)=>typeof expandPackage3Activity==='function'?expandPackage3Activity(activity):[activity])}
function presentationSlide({activity,index,itemIndex,title,prompt,answer,studentHtml,answerHtml,points=1,kind='question'}){return{id:uid('slide'),activityId:activity?.id||'',activityType:activity?.type||'project',activityTitle:activity?.title||title||'',activityIndex:index,itemIndex,kind,title:title||activity?.title||'Aktivita',promptHtml:prompt||studentHtml||'',answerHtml:answer||answerHtml||'',points:Number(points||1)}}
function presentItems(activity,index,items,mapper){return(items||[]).map((item,itemIndex)=>presentationSlide({activity,index,itemIndex,...mapper(item,itemIndex),points:Math.max(1,Math.round(Number(activity.points||items.length||1)/Math.max(1,items.length))) }))}
function wholeActivitySlide(activity,index,variant){return[presentationSlide({activity,index,itemIndex:0,title:activity.title,prompt:`<div class="presentation-worksheet">${renderActivityContent(activity,'student',variant)}</div>`,answer:`<div class="presentation-worksheet key">${renderActivityContent(activity,'key',variant)}</div>`,points:activity.points||1,kind:'activity'})]}
function slidesForActivity(activity,index,variant){const d=activity.data||{},type=activity.type;
  if(type==='matching')return presentItems(activity,index,d.pairs,(item,i)=>({title:`Dvojice ${i+1}`,prompt:`<div class="presentation-term">${esc(item.left)}</div><p>Najděte odpovídající pojem, definici nebo údaj.</p>`,answer:`<div class="presentation-answer-main">${esc(item.right)}</div>`}));
  if(type==='sorting')return presentItems(activity,index,d.items,(item,i)=>({title:`Zařazení ${i+1}`,prompt:`<div class="presentation-term">${esc(item.text)}</div><p>Do které kategorie položka patří?</p>`,answer:`<div class="presentation-answer-main">${esc(item.category)}</div>`}));
  if(type==='truefalse')return presentItems(activity,index,d.items,(item,i)=>({title:`Tvrzení ${i+1}`,prompt:`<div class="presentation-statement">${esc(item.statement)}</div><div class="presentation-choice-row"><span>PRAVDA</span><span>NEPRAVDA</span></div>`,answer:`<div class="presentation-answer-main">${item.answer?'PRAVDA':'NEPRAVDA'}</div>${!item.answer&&item.correction?`<p>${esc(item.correction)}</p>`:''}`}));
  if(type==='multiplechoice')return presentItems(activity,index,d.items,(item,i)=>({title:`Otázka ${i+1}`,prompt:`<h2>${esc(item.question)}</h2><div class="presentation-options">${(item.options||[]).map((option,optionIndex)=>`<div><span>${String.fromCharCode(65+optionIndex)}</span>${esc(option)}</div>`).join('')}</div>`,answer:`<div class="presentation-answer-main">${String.fromCharCode(65+Number(item.answerIndex||0))}. ${esc(item.options?.[item.answerIndex]||'')}</div>${item.explanation?`<p>${esc(item.explanation)}</p>`:''}`}));
  if(type==='shortanswer')return presentItems(activity,index,d.items,(item,i)=>({title:`Otázka ${i+1}`,prompt:`<h2>${esc(item.question)}</h2><div class="presentation-thinking">Promyslete odpověď a připravte stručné zdůvodnění.</div>`,answer:`<div class="presentation-answer-main">${esc(item.answer||'Modelová odpověď není uvedena.')}</div>${item.keywords?.length?`<p>Klíčová slova: ${item.keywords.map(esc).join(', ')}</p>`:''}`}));
  if(type==='wordformation')return presentItems(activity,index,d.items,(item,i)=>({title:`Slovotvorba ${i+1}`,prompt:`<h2>${esc(item.prompt)}</h2><div class="presentation-base-word">${esc(item.base)}</div>`,answer:`<div class="presentation-answer-main">${esc(item.answer)}</div>`}));
  if(type==='errorcorrection')return presentItems(activity,index,d.items,(item,i)=>({title:`Oprava ${i+1}`,prompt:`<div class="presentation-statement error">${esc(item.incorrect)}</div><p>Najděte chybu a navrhněte opravu.</p>`,answer:`<div class="presentation-answer-main">${esc(item.correct)}</div>${item.explanation?`<p>${esc(item.explanation)}</p>`:''}`}));
  if(type==='unitconversion')return presentItems(activity,index,d.items,(item,i)=>({title:`Převod ${i+1}`,prompt:`<div class="presentation-equation">${esc(item.task)}</div>`,answer:`<div class="presentation-answer-main">${esc(item.answer)}</div>${item.working?`<p>Postup: ${esc(item.working)}</p>`:''}`}));
  if(type==='causeeffect')return presentItems(activity,index,d.items,(item,i)=>({title:`Souvislost ${i+1}`,prompt:`<div class="presentation-term">${esc(item.cause)}</div><p>Jaký je odpovídající důsledek?</p>`,answer:`<div class="presentation-answer-main">${esc(item.effect)}</div>`}));
  if(type==='factopinion')return presentItems(activity,index,d.items,(item,i)=>({title:`Klasifikace ${i+1}`,prompt:`<div class="presentation-statement">${esc(item.statement)}</div><p>Fakt, názor, nebo interpretace?</p>`,answer:`<div class="presentation-answer-main">${esc(item.kind)}</div>${item.reason?`<p>${esc(item.reason)}</p>`:''}`}));
  if(type==='coordinates')return presentItems(activity,index,d.items,(item,i)=>({title:`Souřadnice ${i+1}`,prompt:`<h2>${esc(item.place)}</h2><div class="presentation-coordinates">${esc(item.latitude)} · ${esc(item.longitude)}</div><p>${esc(item.question)}</p>`,answer:`<div class="presentation-answer-main">${esc(item.answer)}</div>`}));
  if(type==='safetycase')return presentItems(activity,index,d.items,(item,i)=>({title:`Situace ${i+1}`,prompt:`<div class="presentation-case">${esc(item.scenario)}</div><p>Jak byste bezpečně reagovali?</p>`,answer:`<div class="presentation-answer-main">${esc(item.decision)}</div>${item.reason?`<p>${esc(item.reason)}</p>`:''}`}));
  if(type==='sourceanalysis')return presentItems(activity,index,d.items,(item,i)=>({title:`Pramen · otázka ${i+1}`,prompt:`<div class="presentation-source">${esc(d.source).replace(/\n/g,'<br>')}</div><h2>${esc(item.question)}</h2>`,answer:`<div class="presentation-answer-main">${esc(item.answer)}</div>${item.evidence?`<p>Důkaz: ${esc(item.evidence)}</p>`:''}`}));
  if(type==='secretcode')return presentItems(activity,index,d.items,(item,i)=>({title:`Tajenka ${i+1}`,prompt:`<h2>${esc(item.question)}</h2><p>Po vyřešení použijte ${Number(item.extractIndex)}. písmeno odpovědi.</p>`,answer:`<div class="presentation-answer-main">${esc(item.answer)}</div>`}));
  if(type==='debugcode')return presentItems(activity,index,d.items,(item,i)=>({title:`Ladění ${i+1}`,prompt:`<pre class="presentation-code">${esc(d.code)}</pre><p>Najděte jednu chybu.</p>`,answer:`<div class="presentation-answer-main">${esc(item.error)}</div><pre class="presentation-code answer">${esc(item.correction)}</pre>${item.explanation?`<p>${esc(item.explanation)}</p>`:''}`}));
  return wholeActivitySlide(activity,index,variant)
}
function buildPresentationSlides(project=App.project,variant=project.variant||'A',level=presentationLevel(project)){const activities=presentationPreparedActivities(project,variant,level),slides=[{id:uid('slide'),activityId:'',activityType:'project',activityTitle:project.title||project.topic||'Materiál',activityIndex:-1,itemIndex:-1,kind:'title',title:project.title||project.topic||'Výuková aktivita',promptHtml:`<div class="presentation-title-slide"><span>ACTIVA · ${esc(levelLabel(level))} · varianta ${esc(variant)}</span><h1>${esc(project.title||project.topic||'Výuková aktivita')}</h1><p>${esc(project.goal||'Společné procvičení učiva')}</p><div>${esc(project.subject||'')} ${project.grade?`· ${esc(project.grade)}`:''}</div></div>`,answerHtml:'',points:0}];activities.forEach((activity,index)=>slides.push(...slidesForActivity(activity,index,variant)));slides.push({id:uid('slide'),activityId:'',activityType:'project',activityTitle:'Závěr',activityIndex:activities.length,itemIndex:0,kind:'finish',title:'Hotovo',promptHtml:`<div class="presentation-title-slide finish"><span>ZÁVĚR</span><h1>Shrňte nejdůležitější poznatek.</h1><p>${esc(project.goal||'Co si z dnešní aktivity odnášíte?')}</p></div>`,answerHtml:'',points:0});return slides.slice(0,120)}
function standaloneSafeJson(value){return JSON.stringify(value).replace(/</g,'\\u003c').replace(/-->/g,'--\\u003e')}
function standaloneInteractiveHtml(project,logoDataUrl=''){const variant=project.variant||'A',level=presentationLevel(project),slides=buildPresentationSlides(project,variant,level).map((slide)=>({...slide,id:undefined})),data=standaloneSafeJson({title:project.title||project.topic||'ACTIVA',subject:project.subject,grade:project.grade,variant,level,slides});return`<!doctype html><html lang="cs"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(project.title||'ACTIVA')}</title><style>:root{color-scheme:dark;--gold:#d8b36a;--ink:#10151d;--panel:#1b222d;--muted:#98a3b1}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 15% 5%,#26364a 0,#10151d 42%,#080b10 100%);color:#f5f3ee;font-family:Inter,ui-sans-serif,system-ui,sans-serif;min-height:100vh}.shell{min-height:100vh;display:grid;grid-template-rows:auto 1fr auto}.bar{display:flex;align-items:center;justify-content:space-between;padding:18px 24px;border-bottom:1px solid #ffffff1a;background:#0d1219dd;backdrop-filter:blur(16px)}.brand{display:flex;gap:12px;align-items:center}.brand img{width:38px;height:38px;object-fit:contain}.brand b{letter-spacing:.18em;color:var(--gold)}.progress{color:var(--muted)}main{display:grid;place-items:center;padding:24px}.slide{width:min(1120px,94vw);min-height:min(680px,76vh);background:linear-gradient(145deg,#202936,#141b24);border:1px solid #ffffff1c;border-radius:28px;box-shadow:0 30px 100px #0008;padding:clamp(28px,5vw,68px);display:flex;flex-direction:column;justify-content:center}.kicker{color:var(--gold);font-weight:800;letter-spacing:.16em;text-transform:uppercase;font-size:.78rem}.slide h1,.slide h2{font-size:clamp(2rem,5vw,4.6rem);line-height:1.06;margin:.3em 0}.slide p{font-size:clamp(1.05rem,2vw,1.45rem);line-height:1.55}.answer{display:none;margin-top:28px;padding:24px;border-radius:18px;background:#d8b36a18;border:1px solid #d8b36a66}.answer.show{display:block}.presentation-options{display:grid;gap:14px;margin-top:24px}.presentation-options div{padding:16px 20px;border:1px solid #ffffff24;border-radius:14px;font-size:1.2rem}.presentation-options span{display:inline-grid;place-items:center;width:34px;height:34px;border-radius:50%;background:#ffffff14;margin-right:12px}.presentation-answer-main{font-size:clamp(1.5rem,3vw,2.7rem);font-weight:800;color:#f4d697}.presentation-term,.presentation-statement,.presentation-case,.presentation-equation{font-size:clamp(1.8rem,4vw,3.6rem);font-weight:750;line-height:1.2}.presentation-source,.presentation-code{max-height:300px;overflow:auto;background:#080c12;padding:18px;border-radius:14px;white-space:pre-wrap}.presentation-worksheet{max-height:55vh;overflow:auto;background:#fff;color:#111;padding:22px;border-radius:16px}.controls{display:flex;gap:12px;justify-content:center;padding:18px}.controls button{border:1px solid #ffffff2b;background:#ffffff10;color:white;padding:13px 20px;border-radius:999px;font-weight:750;cursor:pointer}.controls .primary{background:var(--gold);color:#151515;border-color:var(--gold)}@media(max-width:650px){.bar{padding:12px}.slide{padding:24px;border-radius:18px}.controls{flex-wrap:wrap}}</style></head><body><div class="shell"><header class="bar"><div class="brand">${logoDataUrl?`<img src="${logoDataUrl}" alt="Logo školy">`:''}<div><b>ACTIVA</b><div>${esc(project.subject||'Výukový materiál')}</div></div></div><div class="progress" id="progress"></div></header><main><article class="slide"><div class="kicker" id="kicker"></div><div id="prompt"></div><div class="answer" id="answer"></div></article></main><footer class="controls"><button id="prev">← Předchozí</button><button class="primary" id="reveal">Ukázat řešení</button><button id="next">Další →</button></footer></div><script>const DATA=${data};let i=0,revealed=false;const q=s=>document.querySelector(s);function render(){const s=DATA.slides[i];q('#progress').textContent=(i+1)+' / '+DATA.slides.length;q('#kicker').textContent=s.activityTitle||DATA.title;q('#prompt').innerHTML=s.promptHtml||'';q('#answer').innerHTML=s.answerHtml||'';q('#answer').classList.toggle('show',revealed&&!!s.answerHtml);q('#reveal').disabled=!s.answerHtml;q('#reveal').textContent=revealed?'Skrýt řešení':'Ukázat řešení';q('#prev').disabled=i===0;q('#next').disabled=i===DATA.slides.length-1}q('#prev').onclick=()=>{if(i>0){i--;revealed=false;render()}};q('#next').onclick=()=>{if(i<DATA.slides.length-1){i++;revealed=false;render()}};q('#reveal').onclick=()=>{revealed=!revealed;render()};addEventListener('keydown',e=>{if(e.key==='ArrowRight'||e.key===' '){e.preventDefault();q('#next').click()}if(e.key==='ArrowLeft')q('#prev').click();if(e.key.toLowerCase()==='r')q('#reveal').click()});render();<\/script></body></html>`}
