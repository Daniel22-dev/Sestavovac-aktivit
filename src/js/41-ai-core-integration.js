/* ===================== GHRAB AI CORE 1.0.0 · ACTIVA P1 ===================== */
const ACT_AI_APP=Object.freeze({id:'activity-builder',version:'0.5.10'});
const ACT_AI_SCHEMA_ID='activity-builder.project.v1';
const ACT_AI_SCHEMAS=Object.freeze({[ACT_AI_SCHEMA_ID]:{type:'object',required:['title','activities'],properties:{title:{type:'string'},teacherNote:{type:'string'},activities:{type:'array',items:{type:'object'}}},additionalProperties:true}});
const ACT_AI_OPERATIONS=Object.freeze({schema:'ghrab-ai-operations-v1',appId:ACT_AI_APP.id,operations:Object.freeze({'activity-pack-generation':{outputSchemaId:ACT_AI_SCHEMA_ID,defaultModelProfile:'balanced',allowedModelProfiles:['balanced','quality'],inputTypes:['text'],streaming:false,requiredCapabilities:[],expectedOutputs:1,maxOutputTokensHint:32768}})});
function actSchoolMode(){return Boolean(window.GHRAB_PLATFORM?.isSchoolProfile?.())}
function actEnsureAiCore(){if(!window.GHRAB_AI||!window.GHRAB_PLATFORM)throw Object.assign(new Error('Společná AI vrstva se nenačetla. Obnovte stránku přes AI Studio.'),{code:'CONFIGURATION_ERROR'});const state=window.GHRAB_AI.getState?.();if(state?.configured&&state.app?.id===ACT_AI_APP.id)return;window.GHRAB_AI.configure({app:ACT_AI_APP,runtimeConfig:window.GHRAB_PLATFORM.createAiRuntimeConfig({timeoutMs:GEMINI_TIMEOUT_MS,maxRequestBytes:2*1024*1024,maxPartBytes:2*1024*1024,models:{balanced:App.api.model,economy:'gemini-3.5-flash-lite',quality:App.api.model}}),operations:ACT_AI_OPERATIONS,outputSchemas:ACT_AI_SCHEMAS,credentialProvider:async({mode})=>mode==='direct-gemini'?{apiKey:String(App.api.key||''),modelOverride:App.api.model}:null,authProvider:async()=>window.GHRAB_PLATFORM.authProvider(),telemetrySink:event=>window.GHRAB_PLATFORM.recordTelemetry({type:'ai-usage',appId:ACT_AI_APP.id,appVersion:ACT_AI_APP.version,...event})})}
const actLegacyGeminiRequest=geminiRequest;
geminiRequest=async function activityGenerationThroughCore(_model,prompt){if(window.__ACTIVA_TEST_USE_LEGACY_AI__)return actLegacyGeminiRequest(_model,prompt);actEnsureAiCore();const text=String(prompt||'');if(/[\w.+-]+@[\w.-]+\.[A-Za-z]{2,}/.test(text)||/\b(?:\+?420\s*)?(?:\d[\s-]*){9}\b/.test(text))throw new Error('Zdroj obsahuje možný osobní kontakt. Před odesláním AI jej anonymizujte.');const response=await window.GHRAB_AI.generate({operation:'activity-pack-generation',modelProfile:'balanced',instructions:'Jsi zkušený český učitel, didaktik a editor profesionálních pracovních listů. Vrať pouze validní JSON bez markdownu, dodrž zdrojovou věrnost, věcnou správnost a tiskovou použitelnost.',inputParts:[{type:'text',text}],outputSchemaId:ACT_AI_SCHEMA_ID,options:{reasoningHint:'low',maxOutputTokensHint:32768},privacy:{clientAnonymized:true,preflightPassed:true},usageContext:{expectedOutputs:1,userActions:1}});return JSON.stringify(response.result)};
function actApplyServerKeyPolicy(){if(!actSchoolMode())return;window.GHRAB_PLATFORM.enforceLocalKeyPolicy({localStorageKeys:[PERMANENT_KEY],sessionStorageKeys:[SESSION_KEY],onRemoved:()=>{App.api.key='';App.api.storage='server';}});const input=$('#apiKeyInput');if(input){input.value='';input.disabled=true;input.placeholder='Klíč spravuje školní server'}for(const button of $$('#apiModal button'))if(/relaci|trvale|odstranit|smazat/i.test(button.textContent||''))button.hidden=true;const text=$('#apiPillText');if(text)text.textContent='AI připravena · školní server';const open=$('#apiOpenBtn');if(open)open.classList.add('ready')}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',actApplyServerKeyPolicy,{once:true});else actApplyServerKeyPolicy();
